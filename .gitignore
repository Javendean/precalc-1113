__pycache__/
*.pyc
node_modules/
.DS_Store
# Student attempt data must NEVER be committed.
*.session.json
data/

# Throwaway: exercises the pipeline, never published.
build/bank_synth/
# Unverified author output; build/bank/ holds the verified version.
build/bank_raw/
# dist/ IS tracked on purpose: `git subtree push --prefix dist` needs it.
